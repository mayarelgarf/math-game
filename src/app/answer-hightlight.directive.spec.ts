import { AnswerHightlightDirective } from './answer-hightlight.directive';

describe('AnswerHightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerHightlightDirective();
    expect(directive).toBeTruthy();
  });
});
